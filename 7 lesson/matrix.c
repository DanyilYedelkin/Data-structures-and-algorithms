#include <limits.h>#include "matrix.h"int chainMatrixMultiplication(int *p, int n){    n++;    //created new variables    int m[n+1][n+1];    int i, j, k, L, q;    //the number of multiplications are 0, when there's only one matrix (r)    for (i = 1; i < n; i++){        m[i][i] = 0;    }    //int this 'for' - L is chain length. It has varies from 2 to n (length of r).    for(L = 2; L < n; L++){        for(i = 1; i < n - L + 1; i++){            j = i + L - 1;            //m[i][j] has maximum valued (or we could write, as 999999999)            int maxInt = 999999999;            m[i][j] = maxInt;            //m[i][j] = p[0];            for(k = i; k <= j - 1; k++){                q = m[i][k] + m[k+1][j] + p[i-1] * p[k] * p[j];                //checks, if the number of multiplications found less that number will be updated.                if(q < m[i][j]){                    m[i][j] = q;                }            }        }    }    return m[1][n-1];}